node_modules/
vendor/
.tools/
test-results/
playwright-report/
.env

# dist/ is tracked on purpose: the built assets ship inside the Composer
# package, so a LibreNMS host never needs Node. Source maps stay local --
# they are 5.6 MB per build and regenerable with `npm run build`.
dist/**/*.map
